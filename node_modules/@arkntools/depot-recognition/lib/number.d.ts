import Jimp from 'jimp';
import type { RecognizeSimilarityResult } from './similarity';
export interface RecognizeNumberResult {
    img: string;
    text: string;
    value: number;
    warn: boolean;
}
export declare const splitNumbers: ({ splittedImgs, itemWidth, simResults, IMG_SL, }: {
    splittedImgs: Jimp[];
    itemWidth: number;
    simResults: Array<RecognizeSimilarityResult | null>;
    IMG_SL: number;
}) => Array<Jimp | null>;
export declare const recognizeNumbers: (numImgs: Array<Jimp | null>) => Promise<Array<RecognizeNumberResult | null>>;
