import Jimp from 'jimp';
export interface RecognizeSimilarityResult {
    name: string;
    diff: number;
    diffs: Array<[string, number]>;
    diffsTooClose: boolean;
}
export type CompareItemData = Array<[string, Jimp]>;
export declare const getSims: (inputs: Jimp[], imgMap: Map<string, Jimp>, order: string[]) => Array<RecognizeSimilarityResult | null>;
