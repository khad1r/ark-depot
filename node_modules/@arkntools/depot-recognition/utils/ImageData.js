class ImageDataPolyfill {
    constructor(data, width, height) {
        this.data = data;
        this.width = width;
        this.height = height;
    }
}
export default typeof ImageData === 'undefined'
    ? ImageDataPolyfill
    : ImageData;
